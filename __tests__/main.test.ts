import { jest } from '@jest/globals'

//const { run } = await import('../src/main.js')

describe('main.ts', () => {
  beforeEach(() => {})

  afterEach(() => {
    jest.resetAllMocks()
  })

  it('null', async () => {
    expect(true).toBe(true)
  })
})
